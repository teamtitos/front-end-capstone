import React from 'react';

const sortReviews = () => {

  const helpful = (event) => {
    event.preventDefault()
  }

  const newest = (event) => {
    event.preventDefault()
  }

  const relevant = (event) => {
    event.preventDefault()
  }
}

export default sortReviews;